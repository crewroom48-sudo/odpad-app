workflows:
  android-apk:
    name: Android APK Build
    max_build_duration: 60
    environment:
      vars:
        PACKAGE_NAME: "com.odpad.app"
    scripts:
      - npm install
      - npx expo export
    artifacts:
      - dist/**
